#include <stdio.h>

/**
  *main - function that prints it's name and the main function of the program
  *@argc : number of arguements passed during terminal execution
  *@argv : list of arguements passed
  *Return: Always 0 (Success)
  **/

int main(int argc, char *argv[])
{
	argc = 0;
	printf("%s\n", argv[argc]);
	return (0);
}

